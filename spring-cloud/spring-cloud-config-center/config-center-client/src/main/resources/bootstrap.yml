eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9090/eureka/
  cloud:
    config:
      label: main
      profile: default
      # uri: http://192.168.1.120:8081/
      discovery:
        enabled: true
        serviceId: config-center-server